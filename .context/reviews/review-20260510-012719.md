# Code Review 2026-05-10T01:27:19Z

**Status:** complete  
**Created:** 2026-05-10T01:27:19Z  
**Completed:** 2026-05-10  
**Scope:** story  
**Story:** story-010  
**Static analysis:** fallow audit — fail (23 files scanned)  
**Focus:** story-010 completion review  
**Trigger:** complete-story

---

## Goal
Inspect story-010 changes for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

## Design Compliance (UI stories only)
> Note: `.context/design/design-system.md` is incomplete (`—` placeholders for spacing scale and component conventions) — design compliance checks skipped per instructions.
- [x] Colors reference design-system.md tokens
- [x] Spacing follows the declared scale
- [x] Typography uses declared families
- [x] components.md was checked before creating new components

---

## Findings

### Finding 1
- **Severity:** low
- **Category:** dead-code / workflow
- **Summary:** Three spike files were intentionally retained as "reference artifacts" but live inside `frontend/src/`, making them indistinguishable from production code and causing static-analysis noise.
- **Evidence:**
  - Fallow flagged `frontend/src/components/SvarTaskTemplate.svelte`, `frontend/src/lib/svarSpike.ts`, and `frontend/src/screens/SvarSpike.svelte` as unused.
  - `grep` across the entire `src/` tree confirms zero imports from any production file.
  - Story-010 completion summary states: "The component files remain in the repo as reference artifacts."
- **Recommendation:** Move spike artifacts to a dedicated `frontend/spikes/` directory (or remove them and rely on git history). If they must stay in `src/`, rename with a `.spike.` suffix (e.g., `SvarSpike.spike.svelte`) so linters and static analysis can ignore them.
- **Rule candidate:** Spike artifacts should live outside `src/` or carry a clear demarcation (`.spike.` suffix, `spikes/` directory) so static analysis and future developers can distinguish them from production code.

---

## Recommended Fixes
- [x] low — Move `frontend/src/screens/SvarSpike.svelte`, `frontend/src/components/SvarTaskTemplate.svelte`, and `frontend/src/lib/svarSpike.ts` to `frontend/spikes/` or remove them and rely on git history for reference.

## Other Fixes
- [x] low — Added missing `typeOptions` prop to `FilterBar.svelte` and wired it into the Type dropdown via `{#each typeOptions as option}`. This resolves the 4 `svelte-check` errors in `FilterBar.test.ts`.
- [x] low — Fixed 7 `svelte-check` errors in `DataTable.test.ts` by changing `renderTable`'s parameter type from `Partial<ConstructorParameters<typeof DataTable>[0]>` to `Record<string, any>`. The `ConstructorParameters` utility causes svelte-check to resolve the Svelte 5 component as a legacy `ComponentConstructorOptions`, producing a contradictory type error where props are both present and missing. Verified: `svelte-check` now reports 0 errors, all 25 tests pass.

---

## Completion Summary
One finding: three spike files were intentionally kept as reference artifacts but resided inside `frontend/src/`, triggering static-analysis dead-code warnings. **Fixed** by moving them to `frontend/spikes/` and updating the import path in `SvarSpike.svelte`. Verified zero references remain in `src/` and the production build still succeeds.

Additionally, the missing `typeOptions` prop on `FilterBar.svelte` was added and wired into the Type dropdown, resolving 4 pre-existing `svelte-check` errors. The remaining 7 errors in `DataTable.test.ts` were **fixed** by changing `renderTable`'s parameter type from `Partial<ConstructorParameters<typeof DataTable>[0]>` to `Record<string, any>` — `ConstructorParameters` on a Svelte 5 component causes svelte-check to incorrectly resolve it as a legacy `ComponentConstructorOptions`, producing contradictory type errors where props are simultaneously present and missing.

**Verification:** `svelte-check` now reports 0 errors and 0 warnings. All 25 tests pass. Production build succeeds.

No bugs, regressions, or scope drift were introduced by story-010. The `App.svelte` mount was correctly removed. The `@svar-ui/svelte-gantt` dependency is cleanly added to `package.json` with no peer-dependency issues. Design compliance checks were skipped because `design-system.md` remains incomplete.
