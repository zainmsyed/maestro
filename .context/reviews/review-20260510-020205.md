# Code Review 2026-05-10T02:02:05Z

**Status:** complete  
**Created:** 2026-05-10T02:02:05Z  
**Completed:** 2026-05-10  
**Scope:** story  
**Story:** story-011  
**Static analysis:** fallow audit — fail (29 files scanned)  
**Focus:** story-011 completion review  
**Trigger:** complete-story

---

## Goal
Inspect story-011 changes and direct integration points for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

## Design Compliance (UI stories only)
- [x] Colors reference design-system.md tokens
- [x] Spacing follows the declared scale
- [x] Typography uses declared families
- [x] components.md was checked before creating new components

Notes: `GanttView.svelte` uses existing CSS variables (`var(--text)`, `var(--border)`, `var(--surface-glass)`, etc.), uses the declared cozy 8/12/16/24/32px spacing scale, and uses the established display/mono font variables. `components.md` was read before implementation; no production reusable component was added.

---

## Findings

### Finding 1
- **Severity:** low
- **Category:** dead-code / workflow
- **Summary:** Story-010 spike artifacts remain committed and unused under `frontend/spikes/`, continuing to trigger Fallow unused-file findings during story-011 closeout.
- **Evidence:** Static analysis reports unused files: `frontend/spikes/SvarSpike.svelte`, `frontend/spikes/SvarTaskTemplate.svelte`, and `frontend/spikes/svarSpike.ts`. These files are not imported by story-011 production code; `GanttView.svelte` imports SVAR directly and uses `frontend/src/lib/svarBridge.ts`.
- **Recommendation:** Remove the spike artifacts now that story-011 has a production Gantt route, or add a dedicated Fallow/static-analysis ignore for `frontend/spikes/` if the team wants to keep spike references in-tree.
- **Rule candidate:** Spike artifacts should either be removed after the production implementation lands or live in a static-analysis-ignored spike/archive path.

### Finding 2
- **Severity:** low
- **Category:** simplification
- **Summary:** `buildSvarTasks` exceeds the configured complexity threshold and can be simplified without changing behavior.
- **Evidence:** Fallow reports `frontend/src/lib/svarBridge.ts:112` with complexity score 6. The function currently sorts epics, builds a feature-task map, appends summary rows, then appends feature rows.
- **Recommendation:** Extract the feature-task map construction and row assembly into small helpers, or use a flatter transformation pipeline so `buildSvarTasks` becomes orchestration-only.
- **Rule candidate:** Keep data-bridge entry points orchestration-only; move per-pass construction and flattening into named helpers when complexity exceeds threshold.

---

## Recommended Fixes
- [x] low — Remove or static-analysis-ignore `frontend/spikes/SvarSpike.svelte`, `frontend/spikes/SvarTaskTemplate.svelte`, and `frontend/spikes/svarSpike.ts`.
- [x] low — Refactor `buildSvarTasks` into smaller helper functions so it falls below the complexity threshold while preserving the two-pass epic/feature behavior.

## Other Fixes
- [x] Added unit tests for `buildSvarTasks` covering hierarchy, synthetic-epic ordering, start/end fallback behavior, and progress mapping.

---

## Completion Summary
Story-011 implements the intended production Gantt route. Runtime smoke testing surfaced and fixed the SVAR empty-summary crash by closing summary rows with no children. Review found no blocking functional regression in the production route.

Remediation completed: removed unused spike artifacts from `frontend/spikes/`, refactored `buildSvarTasks` into smaller helpers (`buildFeatureTaskMap`, `buildEpicTasks`, `flattenFeatureTasks`) while preserving the two-pass epic/feature behavior, and added `frontend/src/lib/svarBridge.test.ts` for hierarchy, synthetic-epic ordering, start/end fallback behavior, and progress mapping.

Verification after remediation: `npm run check` reports 0 errors and 0 warnings, `npm test` passes 29 tests across 5 files, and `npm run build` succeeds. Design compliance is acceptable for this story: new UI uses existing tokens, declared cozy spacing, and typography variables.
